{
  "name": "prospect-leads",
  "version": "2.0.0",
  "description": "Central de operação de leads — prospecção, funil, WhatsApp",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cheerio": "^1.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
